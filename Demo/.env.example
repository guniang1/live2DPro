# 复制为 Demo/.env 后执行 npm run dev / build
# 后端 HTTP 根（不要末尾斜杠，不要带 /api）。例：后端跑在 9001 则改为 http://127.0.0.1:9001
VITE_API_BASE=http://localhost:8000
# WebSocket 根（可选）。不写则自动由 VITE_API_BASE 推导（http→ws，同主机端口）
# VITE_WS_BASE=ws://localhost:8000
